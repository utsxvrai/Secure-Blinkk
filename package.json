{
  "name": "secure-blink",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:clean": "docker-compose down -v",
    "init-tables": "node scripts/init-tables.js",
    "setup:local": "npm install && npm run docker:up && npm run init-tables",
    "deploy:dev": "serverless deploy --stage dev --region us-east-1",
    "deploy:prod": "serverless deploy --stage prod --region us-east-1",
    "logs": "serverless logs -f api --stage dev --tail",
    "remove": "serverless remove --stage dev --region us-east-1",
    "test": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "bcrypt": "^5.1.1",
    "bcryptjs": "^3.0.3",
    "body-parser": "^2.2.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "joi": "^18.0.2",
    "jsonwebtoken": "^9.0.3",
    "uuid": "^9.0.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "nodemon": "^3.1.11",
    "serverless": "^4.27.1",
    "serverless-dynamodb-local": "^0.2.40",
    "serverless-http": "^4.0.0"
  }
}
